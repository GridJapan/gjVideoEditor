# セキュリティについて

## 前提

このツールは **`127.0.0.1`（自分のPCの中）だけで動くローカルアプリ**です。
インターネットに公開するようには作られていません。認証もありません。

**`ui/server.py` を外部から到達できる場所で動かさないでください。**

## project.json は信用できない入力として扱っています

`.veproj.zip` は人から人へ手渡しされます。他人から受け取ったプロジェクトを開くことを
前提に、次の経路は検証を通しています。

- `canvas.bg` / `meta.title` … ffmpeg のコマンドラインに載る前に検証（フィルタ注入・パス脱出の防止）
- `/api/video` `/api/thumb` … `safe_under_projects()` でプロジェクト外への脱出を遮断
- zip 展開 … 展開先の外に出るパスを拒否

新しく外部の値をコマンドラインやパスに載せるときは、同じように検証を通してください。

## 脆弱性を見つけたら

公開の Issue には書かず、GitHub の **Security → Report a vulnerability**（Private vulnerability reporting）
からご連絡ください。

小さなチームで開発しているため、返信までに数日いただくことがあります。
